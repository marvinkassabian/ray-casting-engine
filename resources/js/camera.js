(function() {
  "use strict";

  ENGINE.namespace('ENGINE.CAMERA');

  ENGINE.CAMERA = (function(module) {

    function Camera() {
    }

    module.Camera = Camera;

    return module;

  })(ENGINE.CAMERA);

})();
