(function() {
  "use strict";

  ENGINE.namespace('ENGINE.CAMERA');

  ENGINE.CAMERA = (function() {

    this.Camera = Camera;

    return this;

    function Camera() {
    }

  }).call(ENGINE.CAMERA);

})();
