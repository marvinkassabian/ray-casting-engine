(function() {
  "use strict";

  ENGINE.namespace('ENGINE.GAMELOOP');

  ENGINE.GAMELOOP = (function(module) {

    module.GameLoop = GameLoop;

    return module;

    function GameLoop() {
    }

  })(ENGINE.GAMELOOP);

})();
