(function() {
  "use strict";

  ENGINE.namespace('ENGINE.GAMELOOP');

  ENGINE.GAMELOOP = (function() {

    this.GameLoop = GameLoop;

    return this;

    function GameLoop() {
    }

  }).call(ENGINE.GAMELOOP);

})();
