(function() {
  "use strict";

  ENGINE.namespace('ENGINE.CONTROLS');

  ENGINE.CONTROLS = (function(module) {

    function Controls() {
    }

    module.Controls = Controls;

    return module;

  })(ENGINE.CONTROLS);

})();
