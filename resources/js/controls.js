(function() {
  "use strict";

  ENGINE.namespace('ENGINE.CONTROLS');

  ENGINE.CONTROLS = (function() {

    this.Controls = Controls;

    return this;

    function Controls() {
    }

  }).call(ENGINE.CONTROLS);

})();
